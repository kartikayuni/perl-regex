Repeated Word Detection

Write a Perl-compatible regex that detects a word repeated twice in a row.
Rules

    Words contain only lowercase letters a-z
    The same word appears twice consecutively
    The words are separated by exactly one space
    The regex should match the whole string
