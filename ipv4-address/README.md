Validate an IPv4 Address

Write a regular expression that matches a valid IPv4 address.
Rules:

    Format: X.X.X.X
    Each X must be a number between 0 and 255
    No leading zeros like 01, 002, etc. (Exception: 0 alone is allowed)
    No extra spaces
    Must match the entire string

