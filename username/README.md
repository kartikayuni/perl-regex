Write a regular expression that matches a valid username with these rules:

    Only lowercase letters (a–z), numbers (0–9), and underscore (_)

    Must start with a letter

    Length must be between 5 and 12 characters

    No spaces allowed

