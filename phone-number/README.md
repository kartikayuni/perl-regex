Write a Perl-compatible regex that validates a phone number with these rules.
Rules

    Country code is optional.
    If present, it starts with +.
    Country code = 1–3 digits.
    After that comes the main number.
    Main number format:
